@ <stdio.hdr>
@ "parser/parser.hdr"
@ "gen/gen.hdr"
@ "lexer/lexer.hdr"

fn main() {
    getToken(); // Initialize global var token
    local expr: ->Expr = parseExpr();
    if (expr && token.kind == EOI) {
        printExprTree(expr);
    } else {
        printf("syntax error\n");
    }
}
