@ <stdio.hdr>
@ "parser/parser.hdr"
@ "gen/gen.hdr"
@ "lexer/lexer.hdr"

fn main() {
    getToken(); // Initialize global var token
    local reg: int = parseExpr();
    if (reg >= 0 && token.kind == EOI) {
        printf("\thalt %%%d\n", reg);
    } else {
        syntaxError();
    }
}
